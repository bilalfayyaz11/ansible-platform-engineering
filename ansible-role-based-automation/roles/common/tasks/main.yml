---
- name: Install common platform packages
  package:
    name:
      - curl
      - vim
      - tree
    state: present
  become: true

- name: Create platform operations directory
  file:
    path: /opt/platform-automation
    state: directory
    owner: root
    group: root
    mode: '0755'
  become: true
